<div class="navbar-collapse collapse show" id="navbarNavDropdown">
  <ul class="navbar-nav ml-auto">
    <li class="d-inline-block">
      <% if Notification.where(customer_id: current_customer.id, checked: false, send_by_admin: false).count > 0 %>
        <%= link_to notifications_path, class:"nav-link text-white" do %>
          <span class="fa-stack"><i class="far fa-bell fa-stack-2x" style="font-size: 0.6em;"></i>
            <span class="fa-3x fa-layers-counter" style="background:gold;color: black;"><%=Notification.where(customer_id: current_customer.id, checked: false, send_by_admin: false).count %></span>
          </span>お知らせ
        <% end %>
      <% else %>
        <%= link_to notifications_path, class:"nav-link text-white" do %>
          <i class="far fa-bell"></i>お知らせ
        <% end %>
      <% end %>
    </li>
    <li class="d-inline-block">
      <%= link_to customer_path(current_customer) , class:"nav-link text-white" do%>
        <i class="far fa-address-card"></i>マイページ
      <% end %>
    </li>
    <li class="d-inline-block">
      <%= link_to contacts_path, class:"nav-link text-white" do %>
        <i class="far fa-clipboard"></i>連絡帳一覧
      <% end %>
    </li>
    <li class="d-inline-block">
      <%= link_to games_path, class:"nav-link text-white" do %>
        <i class="fas fa-asterisk"></i>遊び一覧
      <% end %>
    </li>
    <li class="d-inline-block">
      <%= link_to destroy_customer_session_path, method: :delete, class:"nav-link text-white" do %>
        <i class="fas fa-sign-out-alt"></i>ログアウト
       <% end %>
    </li>
  </ul>
</div>