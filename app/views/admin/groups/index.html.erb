<div class="container">
  <h2 class="col-md-4 offset-md-4 mt-5 text-center under">クラス登録/一覧</h2>
  <div class="ml-4 mt-5" id="new">
    <%= render "form", group: @group %>
  </div>
  <div id="index" class="mt-4 col-md-8 offset-md-2">
    <%= render "index", groups: @groups %>
  </div>
  <div class="col offset-md-8 offset-3 mt-5">
    <%= paginate @groups %>
  </div>
</div>




