<div class="container">
  <h2 class="mt-5">お知らせ一覧</h2>
　<div class="row mt-3">
  <% if @notifications.exists? %>
    <div class="col">
      <%= render @notifications %>
    </div>
  <% else %>
    <div class="col mt-3">
      <p>通知はありません</p>
    </div>
  <% end %>
  </div>
</div>