<div class="container">
  <h2 class="mt-5 offset-lg-1 under col-lg-3 text-center">通知一覧</h2>
　<div class="row">
  <% if @notifications.exists? %>
    <div class="col-lg-10 col offset-lg-1">
      <%= render @notifications %>
    </div>
  <% else %>
    <div class="col mt-3 offset-lg-1">
      <p>通知はありません</p>
    </div>
  <% end %>
  </div>
</div>