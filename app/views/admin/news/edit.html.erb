<div class="container">
  <h2 class="my-5 col-md-6 offset-md-4 text-center under">お知らせ編集</h2>
  <%= form_with(model: @news, url: admin_news_path(@news), class: 'needs-validation', html: {novalidate: true}) do |f| %>
    <%= render partial: "admin/news/form", locals: { f: f } %>
  <% end %>
</div>

